{
  "name": "tailwind-project",
  "version": "1.0.0",
  "description": "Tailwind CSS development project",
  "main": "index.html",
  "scripts": {
    "build-css": "tailwindcss -i ./css/input.css -o ./css/output.css --minify",
    "watch-css": "tailwindcss -i ./css/input.css -o ./css/output.css --watch",
    "dev": "live-server --port=3000",
    "start": "npm run build-css && npm run dev",
    "jekyll:serve": "bundle exec jekyll clean && rm -rf _site && bundle exec jekyll serve"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "datamaps": "^0.5.9",
    "nodemailer": "^6.9.14",
    "resend": "^3.3.0",
    "topojson": "^3.0.2"
  }
}
